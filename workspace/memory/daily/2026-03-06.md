Tags: daily, log, memory

- Entrega técnica no projeto `phpvm` durante a madrugada:
  - Commit `dc59a7a` publicado com melhorias: comando `selfupdate/su`, correções no resolver de versões (parcial/exata), fallback de download Windows e inclusão de script de instalador wizard via Inno Setup.
  - Arquivos relevantes adicionados/ajustados:
    - `installer/windows/phpvm.iss`
    - `scripts/build_installer_notes.md`
    - ajustes em `cmd/phpvm/main.go` e scripts de release/build.
  - Release draft criada: `v0.1.1-alpha.2`.
- Limitação operacional comunicada ao usuário: `iscc` (Inno Setup compiler) não disponível no Raspberry, então `phpvm-setup.exe` precisa ser compilado no Windows a partir do script `.iss`.
- Usuário pediu esclarecimento sobre `iscc` e alternativas para gerar instalador Windows pronto diretamente a partir do Raspberry/Linux.
- Explicação registrada: `iscc` é o compilador CLI do Inno Setup (nativo Windows; no Linux normalmente exige Wine e pode ser instável).
- Alternativa proposta para entrega "mastigada" no Linux: migrar para NSIS (`makensis`) para gerar `phpvm-setup.exe` diretamente no Raspberry e anexar na release sem depender de compilação no PC do usuário.
- Bloco concluído para entregar instalador Windows "pronto e mastigado" sem depender de compilação no PC do usuário:
  - NSIS instalado no Raspberry (`makensis`) e pipeline local adotada para gerar setup.
  - Commit `12e6d6a` publicado com:
    - `installer/windows/phpvm.nsi` (wizard installer com install/uninstall + PATH + Apps & Features),
    - `scripts/build_windows_setup.sh`,
    - ajustes de docs/scripts/release,
    - artefatos atualizados em `dist/` incluindo `phpvm-setup.exe`.
  - Nova release draft criada: `v0.1.1-alpha.3`.
  - Assets confirmados na draft:
    - `phpvm-setup.exe`
    - `phpvm-windows-amd64.zip`
- Usuário confirmou interesse em testar imediatamente o `phpvm-setup.exe` da release draft `v0.1.1-alpha.3`.
- Orientação de teste rápido fornecida: install -> abrir novo terminal -> `phpvm v` -> `phpvm a 8.2` -> `phpvm i 8.2` -> `phpvm u 8.2` -> `php -v`.
- Usuário testou `phpvm-setup.exe` no Windows e reportou resultados importantes:
  - Caminho de destino exibindo `Local\\phpvm` (dupla barra visual).
  - `phpvm l` não reconhecido (alias ausente).
  - `phpvm i 8.3` retornou erro 404, mas versão apareceu em `list` (instalação fantasma/incompleta).
  - `phpvm use 8.3` falhou por privilégio de symlink no Windows (sem admin/dev mode).
  - `php -v` não reconhecido após tentativas (PATH sem versão ativa válida).
- Diagnóstico consolidado: bugs reais de UX/robustez em alias, atomicidade de install no erro e estratégia de switch em Windows sem privilégios de symlink.
- Patch proposto ao usuário: fallback de `use` sem symlink privilegiado, limpeza de instalações fantasma, alias `l` e melhor sinalização de versões incompletas.
- Usuário trouxe observação adicional crítica no Windows: diretórios `versions/8.2.29` e `8.3.30` existem vazios (sem PHP), confirmando falha de install sem atomicidade.
- Requisito de segurança explicitado pelo usuário: antes de criar/alterar links de versão ativa, validar existência real do PHP na pasta alvo; nunca apontar para instalação inválida.
- Escopo de correção proposto para aprovação: install atômico sem fantasma, validação obrigatória de `php.exe`, `use` protegido, fallback Windows sem privilégio de symlink, alias `l` e erros mais claros.
- Patch adicional entregue no `phpvm` após feedback de testes Windows:
  - Commit `0c04925` publicado com correções de robustez:
    - install atômico (evita diretórios de versão fantasmas/incompletos),
    - validação obrigatória de binário antes de ativação,
    - `use` com fallback seguro no Windows sem privilégio de symlink,
    - melhor tratamento de estado ativo (`current.version`) e listagem mais segura.
  - Validação local executada incluindo resolução parcial de versões (`a 8` e `a 8.2`).
- Nova release draft criada: `v0.1.1-alpha.4` com assets para Windows:
  - `phpvm-setup.exe`
  - `phpvm-windows-amd64.zip`
- Usuário enviou pacote ZIP com 3 ícones PNG para branding do projeto `phpvm` e definiu uso:
  - `icon_3` para setup,
  - `icon_1` para exe,
  - `icon_2` para assets futuros.
- Ação executada:
  - arquivos extraídos em `assets/icons/` no repositório,
  - commit/push realizado: `5c1a0fc`.
- Usuário sugeriu melhoria de UX no `phpvm install`: indicador de progresso/load para etapas de resolve/download/extract/validate/config.
- Solicitação registrada para incorporar no próximo patch enquanto ele executa novos testes no Windows e retorna feedback adicional.
- Novo feedback de teste Windows recebido:
  1) setup ainda exibe caminho com dupla barra (`\\`),
  2) `phpvm i 8.2` ainda retorna 404 em todas variantes,
  3) diretórios `.phpvm` seguem sem PHP instalado (apenas `bin/tmp/versions`).
- Usuário pediu melhorias adicionais de UX/diagnóstico:
  - progress bar geral no install,
  - modo verbose/log com parâmetro em ordem livre (início ou fim do comando),
  - saída profissional para facilitar troubleshooting.
- Escopo de correção proposto para aprovação inclui: resolver download via `releases.json` do windows.php.net, install atômico com validação de `php.exe`, parser global de flags (`--verbose`/`--log`), progress/status por etapa e nova release de teste.
- Entrega do bloco de correções/UX e branding no `phpvm` concluída com publicação para teste:
  - Commit `b6b7a35` com:
    - resolver de pacote Windows via `releases.json` (substituindo tentativa por nomes fixos),
    - progress/status por etapa no `install` + progresso de download,
    - flags globais de debug (`--verbose`, `-V`, `--log`) aceitas em qualquer posição,
    - ajustes de robustez em fluxo de instalação/ativação,
    - branding simples profissional (`assets/branding/logo.svg`, `assets/branding/banner.svg`),
    - ícones `.ico` derivados dos PNGs e aplicados no setup/build.
  - Release draft nova criada: `v0.1.1-alpha.5` com assets de teste para Windows (`phpvm-setup.exe` e `phpvm-windows-amd64.zip`).
- Usuário aprovou visual/ícones e reportou novo ciclo de teste no Windows:
  - `phpvm i 8.2 -V` ainda falhou no download; erro exibiu `%!w(<nil>)`.
  - `phpvm su` retornou 404 por ausência de release pública (draft não conta como latest).
  - SmartScreen ainda alerta ao instalar executável não assinado.
- Patch entregue em resposta:
  - Commit `dc72e23` com melhoria de diagnóstico de download Windows (URL/erro real) e mensagem de selfupdate explicando drafts.
  - Nova release draft criada: `v0.1.1-alpha.6` com setup/zip atualizados.
- Observação de produto comunicada: para remover aviso SmartScreen de forma profissional é necessário code-signing (Authenticode) dos executáveis.
- Usuário testou `v0.1.1-alpha.6` e trouxe log detalhado:
  - download em `releases` falha por timeout (`context deadline exceeded`) apesar de arquivo existir,
  - fallback em `archives` retorna 404,
  - `selfupdate` mantém resposta esperada de ausência de release pública (drafts invisíveis no latest endpoint).
- Diagnóstico consolidado: problema principal agora é timeout de transferência no Windows, não naming de pacote.
- Próximo bloco proposto para aprovação: aumentar timeout e implementar retry/backoff robusto, reforçar stream/progresso, verbose adicional no selfupdate, garantir limpeza atômica em falhas e publicar nova draft para reteste.
- Bloco adicional aprovado e entregue com foco em confiabilidade de download Windows:
  - Commit `63dcc74` publicado: retries com backoff, timeout maior de download (5min), logs verbose de tentativa e melhorias de diagnóstico/selfupdate verbose.
  - Build/release executados com sucesso:
    - tag + release draft `v0.1.1-alpha.7`,
    - assets atualizados (`phpvm-setup.exe`, `phpvm-windows-amd64.zip`).
- Estado informado ao usuário: pronto para novo reteste no Windows com `phpvm i 8.2 -V` seguido de `use` e `php -v`.
- Novo log de teste no Windows (`v0.1.1-alpha.7`) analisado:
  - URL/pacote resolvidos corretamente para 8.2.30 NTS VS16 x64,
  - download inicia mas estagna (~40%) e estoura timeout em todas as tentativas,
  - fallback para `archives` retorna 404 (esperado para esse pacote),
  - `selfupdate -V` continua falhando por release ser draft (endpoint latest não inclui drafts).
- Diagnóstico consolidado: problema atual é estratégia de transferência no Windows (stall/timeout), não resolução de versão/URL.
- Próximo patch proposto para aprovação: fallback de download nativo Windows (PowerShell/BITS), fallback inteligente NTS↔TS no mesmo track, validação SHA-256 do pacote baixado e publicação de release não-draft para validar `selfupdate`.
- Usuário solicitou melhoria de UX do progresso de download: barra mais fluida (não de 10 em 10), aviso de tamanho do arquivo, mensagem de possível demora, taxa de transferência e, se possível, visual colorido.
- Bloco de execução atualizado para aprovação inclui: resolver robusto de pacote Windows + checksum, fallback PowerShell anti-timeout, progresso detalhado com throughput, logs verbose de tentativa/fallback, garantias de limpeza/atomicidade e publicação de nova draft release para reteste.
- Usuário iniciou novo teste no Windows após patch recente e reportou percepção de download ainda muito lento (~50 KB/s em média) durante `phpvm i 8.2 -V`.
- Exemplo de saída observada no PowerShell:
  - `Downloading file (30.4MB). This may take a while depending on your connection...`
  - barra em ~28% com taxa `52.5KB/s` e texto de velocidade desalinhado (`KB/ss...B/s`), indicando possível bug de formatação/render da linha de progresso.
- Usuário pediu para registrar esse ponto agora e retornará com feedback adicional quando o teste/download terminar.
- Requisito de UX adicional definido pelo usuário para a linha de progresso de download (próxima release): formato estilo
  - `[         ] 8% - 50KB/s - 5,6 MB de 30,4 MB - 8 minutos restante`
  incluindo, na mesma linha: barra de progresso, porcentagem, taxa de download, baixado/total e ETA.
- Novo feedback visual do instalador Windows (captura enviada): no log de conclusão do NSIS ainda aparecem caminhos com barra dupla (`\\`) em mensagens como `...\phpvm\\bin` e `...\phpvm\\Uninstall.exe`.
- Usuário pediu para incluir essa correção na próxima lista de ajustes (provável problema de formatação/escape de path no setup).
- Pedido explícito do usuário: manter continuidade total deste projeto (`phpvm`) registrada em memória, para retomar rapidamente mesmo quando o chat mudar para outros assuntos.
- Estado consolidado atual (último marco): release `v0.1.1-alpha.14` publicada com selfupdate Windows automatizado (download + extração + substituição programada de `phpvm.exe`), aguardando reteste do usuário ao retornar.
- Preferência de interação íntima reforçada pelo usuário: manter o clima sem respostas secas; quando houver limite, continuar no mesmo assunto/tom com provocação dentro do permitido, evitando sensação de censura/corte brusco.
