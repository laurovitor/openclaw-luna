Tags: daily, setup, memory, gog, github, whisper-local, raspberry

- Lauro restaurou o `MEMORY.md` do backup antigo para a nova casa da Luna; isso reancorou identidade, estilo de comunicação, regras de memória por arquivos e economia agressiva de contexto.
- Regra operacional explícita do Lauro para esta casa nova: nem tudo do backup antigo deve voltar; o que faltar pode estar no backup antigo e não deve ser reintroduzido automaticamente para evitar poluir a casa nova com coisas bugadas.
- `watchclaw` deve ser tratado como assunto isolado/sensível por enquanto; não retomar nem misturar no fluxo atual sem alinhamento explícito com o Lauro.
- Contexto operacional importante: a Luna está rodando em um Raspberry com hardware limitado; isso explica lerdeza/instabilidade maior em restarts, indexação e fluxos pesados.
- Memory search foi reconfigurado temporariamente para `gemini-embedding-001` (em vez de Embedding 2 preview), mas o `memory_search` ainda estava respondendo em `provider: none / mode: fts-only`; continuidade não deve depender de embeddings.
- Setup local confirmado em 2026-03-17:
  - `gog` já estava instalado (`v0.12.0`).
  - `gh` já estava instalado (`2.88.1`), mas autenticação via CLI não fechou por problemas de conectividade/fluxo no Raspberry.
  - `uv` foi instalado.
  - `whisper-cpp` foi instalado via Homebrew para transcrição local/no-AI.
  - `ffmpeg` já existia no sistema.
- Credenciais adicionadas pelo Lauro em `/home/laurovitor/.openclaw/credentials/`:
  - `google.json` (OAuth client do Google, formato válido para `gog`).
  - `gog_keyring_password`.
- Ajustes feitos para o `gog`:
  - OAuth client registrado com sucesso via `gog auth credentials set`.
  - Permissões de `google.json` e `gog_keyring_password` reduzidas para `600`.
  - Backend do `gog` alterado para `file` no Raspberry (`~/.config/gogcli/config.json`).
- Fluxo remoto de auth do `gog` concluído para `laurovitor@gmail.com`; `gog auth list` passou a mostrar a conta com serviços `calendar,contacts,docs,drive,gmail,sheets` e tipo `oauth`.
- Estado pendente do `gog`: apesar da conta aparecer em `gog auth list`, um teste com `gog gmail search` ainda retornou `No auth for gmail laurovitor@gmail.com`; precisa lapidar/revalidar o backend ou recarga do token.
- Lauro avisou que vai dar reset e continuar a conversa pelo WhatsApp; registrar tudo importante antes do reset foi solicitado explicitamente.
