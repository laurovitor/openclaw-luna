Tags: daily, log, memory

- Casa nova/OpenClaw atualizado: regra de organização consolidada por pedido do Lauro — repositórios locais passam a morar fora de `.openclaw`, em `~/Repositories`, para centralização global entre agentes e redução de risco de usar clone errado/desatualizado.
- Regras de canais separadas em arquivos próprios: `memory/roles/whatsapp.md` e `memory/roles/telegram.md`.
- Estrutura da memória foi reorganizada: arquivos diários movidos para `memory/daily/` e `AGENTS.md` ajustado para ler desse caminho.
- Regra de áudio reforçada: todo áudio recebido deve ser transcrito localmente; validação prática feita no Raspberry com `whisper.cpp` local (`/home/laurovitor/whisper.cpp/build/bin/whisper-cli`) transcrevendo com sucesso um áudio `.ogg` de ~34s do Lauro.
- Inventário inicial do ambiente no Raspberry validado: GitHub CLI instalado (`gh 2.46.0`) e depois autenticado com sucesso na conta `laurovitor`; stack base disponível inclui `git`, `node`/`npm`/`pnpm`, `python3`/`pip3`, `go`, `php`, clientes `mysql`/`psql`/`sqlite3`, `supabase` CLI e `railway` CLI.
- `edge-tts` não estava presente no sistema; foi instalado em venv local do servidor (`/home/laurovitor/.venvs/edge-tts`) e validado com vozes PT-BR disponíveis.
- Prompt enxuto de áudio/local-TTS foi publicado como gist público após ajuste de texto (`Linux` genérico em vez de `Linux/Raspberry`) e inclusão da regra de descartar áudio bruto após transcrição validada, mantendo apenas transcrição com nome legível.
- Migração dos workspaces especialistas (“Vulcanos”) da casa antiga `.openclaw-luna` para a nova `.openclaw` executada de forma conservadora: 19 workspaces `workspace-*-ops` copiados/criados na casa nova, com backup prévio do `openclaw.json` em `.openclaw/migration-backups/2026-03-15-vulcanos/`.
- Redundâncias `workspace-devback` + `workspace-devback-ops` e `workspace-devfront` + `workspace-devfront-ops` foram mescladas em destinos canônicos `workspace-devback-ops` e `workspace-devfront-ops`, preservando conteúdo de ambos (incluindo `USER.md` oriundo dos legados).
- `openclaw.json` da casa nova foi atualizado para registrar 19 especialistas Vulcanos, apontando para workspaces em `.openclaw`, e para reativar `allowAgents` dos agentes principais (`main`, `whatsapp`, `telegram`, `slack`).
- Sanidade local validada por script: 23 agentes no config, IDs únicos, nenhum workspace ausente e 19 especialistas permitidos em `allowAgents` dos agentes principais.
- Continuação do refinamento intenso do `deckclaw-template` com foco em replicação visual/comportamental do Velzon nas páginas de referência.
- Commit `a6360b3` publicado: `ref-tables.html` foi refeito para espelhar os modelos da página `tables-basic` (tipos de tabela e variações principais).
- Commit `2e6410c` publicado: `elements.html` foi refeito para espelhar os exemplos de `forms-elements` (inputs, tamanhos, file inputs, input groups, floating/rounded/color picker e variações de composição).
- Diretriz operacional reforçada pelo Lauro: trabalhar 1 página de referência por vez e copiar de forma agressiva o estilo/exemplos do template de origem para uso no `deckclaw-template`.
- Execução recente reforçou conflito de processo: Lauro reclamou novamente quando houve percepção de execução direta em vez de delegação; regra confirmada: usar especialistas/subagentes no fluxo combinado, evitando coding manual no agente principal.
- Iniciada FASE 1 de migração para Tailwind no `deckclaw-template` via `manager-ops` (run `bff701f4...`) com escopo controlado: paleta oficial light/dark + setup Tailwind + migração inicial de `index.html` e `elements.html` com compatibilidade preservada.
- Bug report do Lauro: menus da sidebar ficaram em linha nas páginas de referências recém-criadas (conflito CSS com classes de frameworks externos).
- Hotfix aplicado no `deckclaw-template` com commit `ccacf46`: reforço de `.nav { display: grid !important; }` para impedir override de Bootstrap nas páginas de referência.
- Lauro solicitou mudança de direção para Referências: parar de copiar 1:1 e construir estilo próprio DeckClaw (leve, moderno, consistente), item por item; `icons.html` e `widgets.html` ficam fora do escopo.
- Demanda delegada via `manager-ops` para redesign completo das 11 páginas de referência (elements + ref-*) com validação desktop/mobile e sem regressão no shell.
- Lauro definiu direção técnica para migração do template: padronizar em Tailwind e reduzir CSS próprio ao mínimo pontual, com migração segura para evitar regressões.
- Contexto emocional sensível: Lauro demonstrou frustração intensa com risco de quebra/retrabalho; prioridade operacional reforçada para execução estável, incremental e com validação objetiva.
- Tentativa delegada de lote seguro via `manager-ops` (run `6991f1e3...`) retornou sem alterações práticas novas; resultado considerado insuficiente para avanço real.
- Lauro pediu migração Tailwind de uma vez no projeto inteiro para revisão única; demanda foi delegada via `manager-ops` (run `bc647eaf...`).
- Resultado retornado pelo subagent foi reportado como full sucesso com migração total, porém histórico recente exige validação manual antes de confiar em claims amplos de conclusão.
- Lauro pediu pivot para o repositório `deckclaw`: criar branch `master` e recomeçar com base limpa em Vite + Vanilla JS + Tailwind + Remix Icons + Animate.css + SweetAlert2.
- Execução local concluída no repo `deckclaw`: branch `master` criada/publicada e commit `254aa63` com rebootstrap completo do projeto e paleta light/dark documentada em `PALETTE.md` + `tailwind.config.js`.
- Build do novo projeto base validado com sucesso (`vite build`).
- Solicitação ao `railway-ops` para trocar branch do serviço Railway para `master` foi feita (run `35cb00b3...`), porém retornou bloqueio de credencial/permissão Railway no runtime; troca de branch/deploy não confirmada automaticamente.
- Lauro sinalizou preferência operacional sobre contexto grande: quer compaction real de contexto (como no webchat), não apenas respostas mais curtas; frustração com token/contexto alto foi explicitada.
- Após /reset no WhatsApp, revisão de contexto/regras foi refeita; sessão atual ficou limpa (sem subagentes ativos e sem outras sessões subagent/ACP abertas), com alinhamento explícito para retomar amanhã priorizando delegação via especialistas e controle de consumo de tokens.
- Integração Google da casa nova foi finalmente fechada ponta a ponta via `gog` no Raspberry. Fluxo consolidado: OAuth client salvo em `/home/laurovitor/.openclaw/credentials/google.json`; CLI correta identificada como `@autonia/gog` (npm, não PyPI); `gog` exigiu `GOG_KEYRING_PASSWORD` para o file keyring, resolvido com senha local persistida em `/home/laurovitor/.openclaw/credentials/gog_keyring_password`; callback OAuth foi trocado manualmente por token e importado com `gog auth tokens import`.
- APIs Google habilitadas/validadas no projeto OAuth: Gmail API, Google Calendar API, Google Drive API, Google Docs API, Google Sheets API e People API.
- Validações práticas concluídas com sucesso: Gmail inbox (`gog gmail search`), envio Gmail para `laurovitor@imoblist.com.br` (message id `19cf43f48c4df6be`), Calendar (`gog calendar list`), Drive (`gog drive list`), Contacts (`gog contacts list`) e leitura de Google Doc compartilhado via `gog docs cat` usando o doc id `1FQ5p4M-qg6gWQ6907zpQaEHbO1AJH3hi5n5R3Z7vgoI`.
- Observação operacional do Gmail: com `gog gmail send --body` e `\\n` escapado, o e-mail chegou com quebras literais; preferir `--body-file` ou stdin para corpo formatado corretamente.
- Runbook reutilizável do setup Google/OpenClaw registrado em `memory/topics/google-openclaw-oauth-runbook.md` para futuro tutorial/documentação.
- Preferência nova do Lauro registrada: após `/new` em DM, abrir conversa com uma linha curta de zoeira/charme quando o assunto não for urgente/profissional; se for tema sério/trabalho, responder direto sem gracinha.
- Preferência fixa registrada: ao reportar status/runtime/OpenClaw para o Lauro, incluir `usage` por padrão para evitar que ele precise pedir isso novamente.
- Preferência operacional nova: quando houver algo visual/materializável para validar no site/GitHub, publicar no remoto assim que fizer sentido para o Lauro conseguir validar pelo navegador e pelo GitHub.
- Fluxo operacional reforçado para `deckclaw-api`: Luna não codifica; manager-ops não codifica; ambos apenas delegam/orquestram. Trabalho deve ser quebrado em etapas e microtasks com commit/push a cada etapa concluída, sem paralelismo desnecessário e sem agente pegar nova task antes de concluir/pausar formalmente a atual. Kanban em `.md` na raiz do projeto, editado somente pelo manager, com ownership explícito por task e resumo por estágio incluindo quem fez o quê e tempo gasto por agente/task.
